fastapi>=0.115
uvicorn>=0.34
httpx>=0.28
pydantic>=2.10
pydantic-settings>=2.7
pytest>=8.3
pytest-asyncio>=0.25
cryptography>=43.0
bcrypt>=4.0
jinja2>=3.1
# Headless captcha bridge — generuje tokeny reCAPTCHA v3 dla
# wywołań server-to-server (FB webhook → Medidesk). Wymaga osobnej
# instalacji binariów Chromium: `playwright install --with-deps chromium`
# (wpięte w buildCommand render.yaml).
playwright>=1.40
